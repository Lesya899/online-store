server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Lesya899/configs.git
          username: ********
          password: ********
          default-label: main
          search-paths: "{application}"
